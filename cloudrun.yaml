apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: sbgcrun-frontend
  annotations:
    #   run.googleapis.com/ingress: all
    run.googleapis.com/invoker-iam-disabled: "true"
    run.googleapis.com/maxScale: "2"
    run.googleapis.com/ingress: internal-and-cloud-load-balancing
    # 6-1でコメントを外す。デフォルトURLを無効化する
    # run.googleapis.com/default-url-disabled: "true"
    # 6-9でコメントを外す。BinaryAuthの有効化
    # run.googleapis.com/binary-authorization: default
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/maxScale: '2'
        run.googleapis.com/execution-environment: gen2
        # 5-4でコメントを外す。VPCネットワークの設定
        # run.googleapis.com/network-interfaces: '[{"network":"sbgcrun-app", "subnetwork":"sbgcrun-app"}]'
        # run.googleapis.com/vpc-access-egress: all-traffic
    spec:
      containerConcurrency: 10
      timeoutSeconds: 300
      serviceAccountName: sbgcrun-app-frontend@PROJECT_ID.iam.gserviceaccount.com
      containers:
      - image: frontend
        env:
          - name: BACKEND_URL
            value: "https://sbgcrun-backend-992104480811.asia-east1.run.app" # FIXME: Change BACKEND_FQDN value after backend resources is created
        ports:
        - name: http1
          containerPort: 8080
        resources:
          limits:
            cpu: 1000m
            memory: 512Mi
        startupProbe:
          initialDelaySeconds: 15
          timeoutSeconds: 2
          periodSeconds: 2
          failureThreshold: 60
          tcpSocket:
            port: 8080
